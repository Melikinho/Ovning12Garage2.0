document.querySelector('#unpark').addEventListener('click', function (e) {
    console.log(e);
    e.preventDefault();

    var id = document.querySelector('#Id').value;
    console.log(id);

    fetch('')
});